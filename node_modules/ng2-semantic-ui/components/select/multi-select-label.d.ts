import { ElementRef, Renderer, ViewContainerRef, EventEmitter } from "@angular/core";
export declare class SuiSelectMultiLabel {
    private el;
    private renderer;
    classes: boolean;
    private _transition;
    constructor(el: ElementRef, renderer: Renderer);
    useTemplate: boolean;
    click(event: MouseEvent): boolean;
    readValue: (value: any) => string;
    value: any;
    selected: EventEmitter<any>;
    viewContainerRef: ViewContainerRef;
    deselectOption(): boolean;
}
