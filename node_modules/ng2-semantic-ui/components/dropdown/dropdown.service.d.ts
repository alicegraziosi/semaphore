import { EventEmitter, ElementRef } from '@angular/core';
import { SuiTransition } from "../transition/transition";
export declare const KeyCode: {
    Left: number;
    Up: number;
    Right: number;
    Down: number;
    Escape: number;
    Enter: number;
    Space: number;
    Backspace: number;
};
export declare class SuiDropdownService {
    private _isOpen;
    isDisabled: boolean;
    autoClose: string;
    onToggle: EventEmitter<boolean>;
    isOpenChange: EventEmitter<boolean>;
    dropdownElement: ElementRef;
    menuElement: ElementRef;
    private closeDropdownBind;
    private keybindFilterBind;
    private _selectedItem;
    itemClass: string;
    itemSelectedClass: string;
    itemDisabledClass: string;
    transition: SuiTransition;
    isVisible: boolean;
    isOpen: boolean;
    toggle(): void;
    bindDocumentEvents(): void;
    unbindDocumentEvents(): void;
    private closeDropdown(event);
    private keybindFilter(event);
    selectedItem: Element;
    keyPress(keyCode: number): void;
    selectNextItem(): void;
    selectPreviousItem(): void;
}
