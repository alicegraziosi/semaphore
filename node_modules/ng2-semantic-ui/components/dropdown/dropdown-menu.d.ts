import { ElementRef, Renderer } from '@angular/core';
import { SuiDropdownService } from './dropdown.service';
export declare class SuiDropdownMenu {
    el: ElementRef;
    renderer: Renderer;
    private _service;
    private _transition;
    service: SuiDropdownService;
    constructor(el: ElementRef, renderer: Renderer);
}
