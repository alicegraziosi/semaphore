import { EventEmitter, Renderer, AfterViewInit } from '@angular/core';
export declare class SuiMessage implements AfterViewInit {
    private renderer;
    dismissible: boolean;
    onDismiss: EventEmitter<SuiMessage>;
    private messageComponent;
    private _transition;
    constructor(renderer: Renderer);
    ngAfterViewInit(): void;
    private dismissed;
    private dismiss();
    private classes;
}
export declare const SUI_MESSAGE_DIRECTIVES: typeof SuiMessage[];
